import { ChevronRight, Download } from "lucide-react";
import { Link } from "react-router-dom";
export function SurahRow({ chapter, downloaded }) { return <Link to={"/surah/" + chapter.id} className="surah-row"><span className="surah-number">{chapter.id}</span><span className="surah-names"><strong>{chapter.nameSimple || chapter.transliteratedName}</strong><small>{chapter.translatedName} · {chapter.revelationPlace} · {chapter.versesCount} ayahs {downloaded && <Download size={13} aria-label="Meaning downloaded"/>}</small></span><span className="arabic surah-arabic" lang="ar" dir="rtl" translate="no">{chapter.nameArabic}</span><ChevronRight className="chevron"/></Link>; }
