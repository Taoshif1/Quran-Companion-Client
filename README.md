# Quran Companion Client

The React/Vite frontend for the Phase 1 Quran reader foundation. It supports the web, PWA, and future Capacitor-native builds from one codebase.

Copy `.env.example` to `.env` if the API is not running at its default local address, then run `npm run dev`.
